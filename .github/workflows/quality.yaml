name: Quality

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  golangci:
    name: GolangCI
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: GolangCI
        uses: golangci/golangci-lint-action@v9
