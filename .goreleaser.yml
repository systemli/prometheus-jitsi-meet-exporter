before:
  hooks:
    - go mod tidy
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarm:
      - "6"
      - "7"
    ldflags:
      - -s -w
dockers:
  -
    goos: linux
    goarch: amd64
    image_templates:
      - "Movion/prometheus-jitsi-meet-exporter:{{ .Tag }}"
      - "Movion/prometheus-jitsi-meet-exporter:{{ .Major }}"
      - "Movion/prometheus-jitsi-meet-exporter:{{ .Major }}.{{ .Minor }}"
      - "Movion/prometheus-jitsi-meet-exporter:latest"
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}-next"
changelog:
  skip: true
